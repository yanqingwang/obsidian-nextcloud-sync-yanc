# Nextcloud sync YANC（フォーク）

> **本プラグインはフォーク「Nextcloud sync YANC（v1.0.0）」です。**
> 上流 [siosig/obsidian-nextcloudsync](https://github.com/siosig/obsidian-nextcloudsync) の v0.7.43 からフォークしています。
> 隠しファイル（`.env`、`.gitignore`、…）およびドットフォルダ（`.git`、`.hidden`、…）の自動除外を追加しています（いずれも既定で有効、個別に無効化可能）。上流はこの変更のマージを拒否したため、ここに保守フォークとして配置しています。コア同期エンジンの一切の功績は元の作者 Daisuke ITO / siosig に帰属します。

**複数のデスクトップ・モバイル端末をまたいで作業しているすべての方へ朗報です。**

あなたが払うコストは、初回インストール時に Vault インデックスの作成完了を待つことだけです。その時点以降は、次が得られます：

- **書いた内容が決して失われない** — マージロジックにより、同じノートを複数端末で同時に編集しても、Vault がクリーンで整合した状態に保たれます。
- **同期が背景へ溶け込む** — Nextcloud の高速な差分同期により、同期にかかる時間をまったく意識しなくなります。
- **Nextcloud 以外でも動作** — 機能はやや減りますが、標準的な WebDAV サーバーに対しても同様に動作します。

---

Obsidian Vault と Nextcloud を双方向同期します。汎用的な WebDAV ではなく、**Nextcloud 専用**に作られています。

多くの「WebDAV 同期」プラグインは、サーバーを単なるファイル置き場として扱います。更新日時を比較し、ファイルをコピーして、うまくいくことを祈るだけです。**Nextcloud Sync** はそうではなく、Nextcloud 自身の API（Capabilities、ファイル ID、チェックサム、バージョン、ロック、Login Flow v2）を活用して、同期を *安全* に・*高速* に・*摩擦なく* します。同時に、それらの API が利用できない場合は素の WebDAV へグレースフルにフォールバックします。

> 英語版（原文）は [`README.md`](README.md) を参照してください。

---

## 💬 あなたのフィードバックがこのプラグインを形づくります

このプラグインはまだ若く、一部の動作は粗削りです。**つまずいた点をぜひ教えてください — 本当に助けになります。** 何かが壊れた、何かが足りない、あるいは使ってみての感想でも構いません。ぜひ聞かせてください（特に感想は私の励みになります！）：

- 🐛 **バグを報告する** → [GitHub Issues](https://github.com/siosig/obsidian-nextcloudsync/issues)
- 🙋‍♂️ **機能を要望する／感想を共有する** → [GitHub Discussions](https://github.com/siosig/obsidian-nextcloudsync/discussions)

---

> ⚠️ **お知らせ：設定をシンプルにしていきます。** これから数日かけて、利用価値の少ないオプションを段階的に削っていきます。プラグインを単純で誤設定しにくい状態に保つためで、適切な動作は自動的に導出されます。今まで見えていた設定が消えていたら、それは意図的なものです — プラグイン側が最適な値を自動で選びます。

---

## このリリースでの変更点（1.0.3）

> **1.0.3** は、手動パスワード署名を確実に機能させる更新です。過酷なモバイルネットワーク（HarmonyOS 出境易／卓易通）でブラウザログインが失敗する場合の代替手段です。

- **手動署名が即時反映** — 修正：起動時に初期化されたエンジンがパスワード null のクライアントを保持したままになり、後から入力したアプリパスワードが再起動まで使われませんでした。「Sync now」は資格情報の変化を検出してエンジンを再構築します。
- **アプリパスワードの貼り付けフィールド** — 確定時に Obsidian の Secret Storage へ暗号化保存。シークレットが DOM や `data.json` に残ることはありません。
- **「Verify & connect」ボタン** — 入力した資格情報でサーバーに接続検証し、実用的なエラー（誤パスワード → HTTP 401、メンテナンスモード、通信障害の翻訳）を報告し、成功時に同期を有効化します。

## このリリースでの変更点（1.0.2）

> **1.0.2** は、過酷なモバイルネットワーク環境でブラウザ署名が失敗しないようにする更新です。HarmonyOS の出境易／卓易通 Android コンテナで Obsidian を実行した際に報告された「Login failed: SocketException」の原因に対応します。

- **ログイン要求のリトライとハードタイムアウト** — Login Flow v2 の初期 POST は一時的な通信障害を最大 3 回バックオフ付きでリトライし、すべてのログイン要求に 30 秒のハードタイムアウトが付きます。
- **寛容な承認ポーリング** — 失敗したポーリングはログインを中断せず「pending」として線形バックオフで再試行し、連続 15 回の通信失敗でのみ諦めます。承認ブラウザから復帰直後のソケット切断の連発でも、承認済みの署名がキャンセルされなくなります。
- **実用的なエラーメッセージ** — ネイティブ例外文字列（「SocketException: Connection reset」「SSLException」など）を、手動アプリパスワードによる代替手順への言及付きで翻訳します。

- **本プラグインは siosig/obsidian-nextcloudsync のフォーク（上流 v0.7.43 ベース）です** — 同期エンジンはそのまま引き継いでいます。
- **隠しファイルの除外** — 名前が `.` で始まるファイル（`.env`、`.gitignore`、`.DS_Store` など）はアップロードもダウンロードも行われません。既定で有効。設定の「Exclude hidden files」で無効化できます。
- **ドットフォルダの除外** — 名前が `.` で始まるフォルダ（`.git`、`.hidden` など）とその配下すべてを同期しません。常時有効の `.git`/`.trash` 除外をすべてのドットフォルダに拡張したものです。既定で有効。設定の「Exclude dotfolders」で無効化できます。

全リリースの完全なバージョン履歴は **[変更履歴（CHANGELOG）](CHANGELOG.ja.md)** を参照してください。

---

## なぜ Nextcloud 特化なのか（汎用 WebDAV との違い）

| 観点 | 汎用 WebDAV プラグイン | **Nextcloud Sync** |
|------|----------------------|--------------------|
| 変更検出 | 更新日時（時刻ずれに弱く、誤った再アップロードを招く） | **内容ハッシュ** ＋ Nextcloud の `sync-token` / checksums capability による真の差分同期 |
| リネーム / 移動 | 削除＋再作成（履歴を失い、全デバイスで再ダウンロード） | **ファイル ID（`OC-FileId`）追跡** — リネームは全デバイスでリネームのまま、履歴を保持 |
| 削除の安全性 | ハード削除 | **Nextcloud ゴミ箱** 経由 — 復元可能で、不可逆な `DELETE` は決して行わない |
| セットアップ | アプリパスワードを手動でコピー＆ペースト | **Login Flow v2** — ブラウザで承認するだけで認証情報を自動発行・保存 |
| 大容量ファイル | 単一 `PUT` でスキップまたは失敗 | **チャンクアップロード** — 分割・再開可能・チェックサム検証付き |
| 同時編集 | 他者が書き込まないことを祈る | **楽観的同時実行** — すべての更新が `If-Match` プリコンディションを伴うため、他デバイスが変更したリモートはロック往復なしでコンフリクトに変換される（ロストアップデートなし）。サーバー側の **Files Locking** はオプトインで併用可能 |
| 誤操作からの回復 | なし（手元のコピーが全て） | **サーバーバージョン履歴** — Obsidian 内から過去の任意のリビジョンを閲覧・復元 |
| サーバー停止時 | 不可解なエラー／中途半端な書き込み | **メンテナンスモード検出**（`/status.php`）と整形済みの Nextcloud エラーメッセージ |
| 機能の認識 | なし | **Capabilities 探索**（`/ocs/.../capabilities`）— サーバーが対応する機能だけが有効化される（Progressive Enhancement） |

非 Nextcloud の WebDAV サーバーを指定した場合は、Nextcloud 専用機能を自動的に無効化し、標準的な再帰 WebDAV 同期へフォールバックします — 機能は減りますが、そのまま動作します。

---

## 機能

### コア同期
- Obsidian と Nextcloud の**双方向同期**。
- **ハッシュベースの差分同期** — 変更されたファイルのみを転送（全件再スキャンなし）。1,000 ファイル規模の Vault でも数秒で収束します。
- **アトミック書き込み** — 転送中に中断されたダウンロードが、書きかけや 0 バイトのファイルを Vault に残すことは決してありません。
- Nextcloud ファイル ID による**リネーム / 移動の追跡** — ノートを移動しても全デバイスで再アップロードしません。
- **ゴミ箱経由の削除** — リモート削除は Nextcloud のゴミ箱を使用します（復元可能）。削除をローカル Vault に適用する際は、挙動を一つに固定せず **Obsidian の「削除されたファイル」設定**（システムのゴミ箱／`.trash` へ移動／完全削除）に従います。Vault が追跡するノートの外にあるフォルダやファイル（例: 設定フォルダ配下のファイル）も処理されます。
- **Vault ごとの設定** — 各 Vault が別々の Nextcloud サーバー／アカウントを対象にでき、状態が互いに混ざりません。
- **除外フォルダ** — 同期しない Vault 相対フォルダを登録できます（アップロードもダウンロードもせず、サーバー上にも作成しません）。`.git` のようなバージョン管理／ツール用フォルダや、端末ローカルに留めたい大容量メディアに有用です。マッチはフォルダ境界での前方一致なので、`Attachments` は `Attachments/` 配下すべてを除外しますが `Attachments-old` は除外しません。**設定 → Excluded folders** でパスを入力するかフォルダピッカーで追加・削除できます。ドットフォルダ（`.git`、config フォルダの `plugins/`、プラグイン自身の状態）は従来どおり自動で除外され、本リストはその上に重ねる追加分です。
- **定期自動同期**（間隔は設定可能、`0` で手動のみ）と **Sync now** コマンド。
- **ファイル変更時の同期（ウォッチモード）** — 任意で、ファイル**やフォルダ**を作成・編集・削除・リネームした直後に即時同期します（ファイル編集は入力を止めてから約 2 秒のデバウンス。削除・リネーム・フォルダ操作は即時に伝播）。伝播中はステータスバーに表示されます。設定で ON/OFF を切り替え可能。定期間隔と併用でき、デスクトップ限定です。
- **粘り強いリトライ** — 失敗したファイルはスキップしてキューに積み、次回同期で指数バックオフ付きに再試行します。切断された Wi-Fi 接続は自動的に再開されます。
- **標準 WebDAV フォールバック** — 任意の WebDAV サーバーで動作し（再帰）、Nextcloud 機能は自動的に無効化されます。
- **同期ステータスダイアログをステータスで絞り込み** — ステータスダイアログにはチェックボックスの行（Uploaded、Downloaded、Deleted、Merged、Conflicted、Local wins、Remote wins、Error）があり、たとえばコンフリクトだけに集中できます。既定はすべて ON。選択は保存され、Obsidian の再起動を跨いで永続し、すべてのセクションに適用されます。
- **ファイルをリモート版と比較する** — エクスプローラ内の任意のファイルを右クリック（モバイルでは長押し、またはコマンド「Compare with remote」を実行）して、ローカルとリモートの更新日時・チェックサム（一致／不一致バッジ付き）・行 diff を比較するポップアップを開けます。狭い画面では diff が縦積み表示になります。その場で **push**（ローカルでリモートを上書き）または **pull**（リモートでローカルを上書き）により差分を解決でき、いずれも確認を挟みます。
- **端末別ログ** — Vault ルートに出力される単一のオプトインログ `nextcloud-debug_<device>.txt`。端末ごとに名前が付くため、複数の端末が互いを上書きすることはありません（他端末のログをこの Vault に同期集約して見比べることもできます）。各行はタイムスタンプ付きでプラグインのバージョンを含み、ログを ON にした瞬間に全設定のスナップショットを書き込み、以降は同期の各操作を記録します。コンソールのないモバイルでのトラブルシューティングに有用です。注意：Obsidian は *設定 → ファイルとリンク → すべてのファイル拡張子を認識* が ON でないと `.txt` を表示せず、Quick Switcher にも出ません（OS や Nextcloud 上でも開けます）。終わったら OFF にしてファイルを削除してください。
- **Vault インデックスをリセットする** *(設定 → Maintenance)* — この端末の同期トラッキングインデックスを初回インストール時の状態へ戻し（確認を挟みます）、次回同期で全件を再スキャンさせます。Vault やリモートのファイルは一切削除しません。不整合な同期状態からの復旧に使ってください。
- **リモートの状態に合わせる（Mirror from remote）** *(設定 → Maintenance)* — この端末の Vault をリモートの現在状態に完全に一致させます。リモートにあるものをすべてダウンロードし、リモートに無いローカルのファイル／フォルダを削除します（Obsidian のゴミ箱設定に従うため復元可能）。実行前に「ダウンロード予定数／削除予定数」を確認ダイアログで提示します。ローカルの未同期の変更は破棄されます。大量削除の安全上限はバイパスします（あなたが明示的にリモートを正と宣言したため）が、リモート一覧を完全に取得できない場合は何も削除せず中止します。他の同期ツールから移行した端末をリモートに従わせたいときに便利です。

### コンフリクト安全性（内容を決して失わない）
- **ファイル種別ごとの戦略。** どの拡張子を「自動マージファイル」とするか（既定は Markdown・テキスト・主要なコード拡張子）と、種別ごとのコンフリクト戦略 —「自動マージファイルの戦略」と「その他ファイルの戦略」（画像・PDF・設定 JSON など）— を選びます。コンフリクトは必ず決着し、保留／エラーのモードはありません。
- **Merge**（`reconcile-text` / diff3。自動マージファイルの既定）— 異なる領域の編集を統合し、両者が重複しない行を変更した場合は YAML フロントマターも含めて統合します。テキスト競合はコンフリクトマーカーとして書き込み、非テキストファイルは一切触らずフラグのみ立てます（マーカーで破壊しません）。
- **決定的戦略**（それ以外）: **Latest modified**（新しい側を採用 — その他ファイルの既定）、**Biggest size**（大きい側）、**Local wins**、**Remote wins**。サイズ/更新時刻が同値の場合は両側不変で次回同期に再評価します。負け側の復元のため Nextcloud のバージョン履歴を有効にしておくことを推奨します。*Compare with remote* で個別に手動解決もできます。
- ステータスバーの**コンフリクトバッジ** — 未解決コンフリクトの件数を表示します（0 で通常表示に戻ります。`#conflict` タグ検索との相性が良いです）。

### Nextcloud 強化機能
- **Login Flow v2** — アプリパスワードを手動で発行・貼り付けする代わりに、ブラウザでの承認でセットアップします。認証情報は Obsidian の secret credentials ストアに保存され、`data.json` に**平文で書かれることはありません**。
- **サーバーバージョン履歴** — アクティブなノートについて、サーバーが保持するすべてのリビジョンを新しい順に一覧し、確認のうえ任意のものをアトミックに復元します。復元された内容は、無限のコンフリクトループを引き起こすことなくクリーンに同期されます。
- **チャンクアップロード** — チャンク閾値を超える大きな添付（画像、PDF、音声）は分割され、再開可能にアップロードされます。中断されたアップロードが不完全なファイルを公開することはなく、完了時にチェックサムで検証されます。メモリを守るための別個の絶対上限 `maxFileSizeMB` も備えており、**アップロードとダウンロードの双方向**で適用されます（ダウンロードのサイズはサーバーの PROPFIND から取得するため、本文を取得する前にスキップします）。
- **ロックなしのロストアップデート防止** — すべての更新に常時有効の `If-Match` プリコンディションを付与します。他クライアントが変更したリモートは 412 を返し、エンジンがそれをコンフリクト（リモート取得＋解決）に変換します。これによりファイルごとの WebDAV LOCK/UNLOCK 往復を不要にし、サーバー側ファイルロックは意図的に一切使用しません。

---

## モバイル（iOS / Android）

モバイルに対応しており、プラットフォームを考慮したいくつかの差分があります（デスクトップの挙動は変わりません）：

- **モバイルでは自動同期が既定で OFF です。** OS がバックグラウンドのタイマーを停止するため、定期自動同期と「ファイル変更時の同期」は無効化されます（グレーアウト）。**Sync now** を使うか、**起動時に同期**（0.7.11 以降は全プラットフォームで既定 ON）に任せて、アプリ起動の数秒後に一度同期してください。
- **モバイルでは大容量ファイルがスキップされます** — 「最大ファイルサイズ」上限（`0` で無制限）を超えるものは、**アップロード・ダウンロードの双方向**で、メモリ不足によるクラッシュを避けるためスキップされ、スキップは報告されます。
- **モバイルには進捗 UI がありません** — エラー通知のみが表示されます。
- **ネットワーク並列数** は設定可能で、初回の既定値は端末の利用可能メモリから導出されます — デスクトップとモバイルで一律、プラットフォーム固有の分岐はありません: RAM 8 GB 以上で **16**、4 GB 以上で **8**、それ未満で **4**、端末がメモリ量を報告しない場合（モバイルに多い）は **3**。転送は上限付きの並列度で実行され — この数と、同時にやり取り中のバイト総量の予算（モバイルでは小さめ）の両方で上限化され、大きなファイルがメモリを使い切ることはありません — また、同一フォルダへのアップロードは直列化され、サーバー側のロック競合を回避します。
- **Wi-Fi のみで同期** はセルラー時にスキップします（Android／デスクトップ）。**iOS では利用不可**（ネットワーク種別の API が無い）で、トグルは無効化されます。
- **モバイルでは Sync now が結果通知を表示します**（アップロード／ダウンロード／コンフリクト、または「すでに最新です」）— ステータスバーが無いためです。サインインしていない状態でタップしても無効のままで、設定画面には明確な「まだサインインしていません」バナーが表示されます。
- デバッグモード（診断ログ）はモバイルでも利用可能で、同期の挙動は変えません。

## 動作要件

- **Obsidian** `1.11.4` 以降（本プラグインは `1.11.4` で導入された secret-storage API を使用します）。デスクトップ（Electron）とモバイル（iOS / Android）に対応します。
- Nextcloud 固有機能には **Nextcloud** Hub 26 「Winter」（サーバー `33`）以降を**推奨**します。これより古い Nextcloud サーバーはもはやブロックされません — 接続・同期は可能ですが、設定画面に推奨バナーが表示され、一部の機能が制限される場合があります。素の WebDAV サーバーはコア同期にフォールバックします。
- Nextcloud アカウント。**Login Flow v2**（推奨）または手動発行の**アプリパスワード**（メインパスワードは決して使わない）で認証できます。

---

## インストール

### コミュニティプラグインブラウザから（推奨）
1. Obsidian で **設定 → コミュニティプラグイン** を開きます。
2. 制限モードを解除し、**閲覧** をクリックして **Nextcloud Sync** を検索します。
3. **インストール** し、続けて **有効化** します。

### 手動インストール
1. 最新の [GitHub Release](../../releases) から `main.js` と `manifest.json`（あれば `styles.css`）をダウンロードします。
2. それらを `<YourVault>/.obsidian/plugins/nextcloud-sync/` にコピーします。
3. Obsidian をリロードし、**設定 → コミュニティプラグイン** で **Nextcloud Sync** を有効化します。

---

## はじめかた

1. **設定 → Nextcloud Sync** を開きます。
2. **サーバー URL** を入力します。**ホストだけでは不十分**で、WebDAV エンドポイント全体
   （`https://<host>/remote.php/dav/files/<user>/`）を指定してください。`https://cloud.example.com` のように
   ホストだけを入れると **HTTP 405** で失敗します。`<user>` は Nextcloud のユーザー ID（通常メールアドレス
   ではありません）。末尾にサブフォルダ（例: `.../<user>/Documents`）を付けると、その配下へ同期できます。
3. 認証します：
   - **推奨:** **ブラウザでログイン**（Login Flow v2）をクリックし、ブラウザで承認すると、認証情報が自動入力・保存されます。**または**
   - **ユーザー名** と手動発行の**アプリパスワード**を入力します。
4. （任意）自動同期間隔、**ファイル変更時の同期**（ウォッチモード）、自動マージのオプションを調整します。
5. **Sync now** コマンドを実行します（または定期同期を待ちます）。初回実行では Vault とリモートの全件スキャンを行い、必要なものを転送します。以降の同期は差分的になります。

あなたの Vault は Nextcloud 側で Vault 名のフォルダへ同期され、複数の Vault がきれいに分離された状態に保たれます。

---

## FAQ

**アカウント全体へのフルアクセスを許可する代わりに、範囲を限定した Nextcloud の Public Link Share を使えますか？**

はい、使えます。アカウント全体にアクセスできるアプリパスワードをプラグインに渡したくない場合、Nextcloud 側で特定のディレクトリだけを**ユーザー名/パスワード付きの Public Link Share** として共有し、そのリンクをプラグインに設定できます。

- **サーバー URL:** `https://<host>/public.php/webdav/`
- **ユーザー名:** 共有トークン（共有リンクの `/s/` 以降の部分）
- **アプリパスワード:** その共有のパスワード

ここでよくある混同が一点あります。アプリパスワードを保存するために **Link…** をクリックすると、**ID** と**値**の入力を求められます。この **ID は Obsidian の暗号化された Secret Storage 内のローカルな保存キーであり、サーバーには一切送信されません**。そのため何でも構いません（例: `default`）。小文字英数字とハイフンのみに制限されているのは Obsidian 本体側の仕様であり、このプラグインが制御しているものではありません。実際の共有パスワードは**値**の欄に入力し、こちらは任意の文字列を使用できます。共有トークンは **ID 欄ではなく Username 欄**に入力してください。

補足: バージョン履歴の復元やチャンクアップロードは、基本同期の上に乗った付随的な便利機能であり、Public Link Share のエンドポイントに対しては検証していません。動作しない可能性があります。基本的なアップロード・ダウンロード同期には影響しません。

---

## Nextcloud サーバー側機能の有効化

1 つの強化機能がサーバー側の Nextcloud アプリに依存します。**Nextcloud 管理者が一度だけ有効化**すれば済みます。プラグインは capabilities API を通じて検出します — アプリが無ければ、該当機能は単に無効のままになります（エラーにはなりません）。

> **ロスト・アップデート対策に設定は不要です。** プラグインはアップロード時に常に `If-Match` 条件を送るため、別クライアントがサーバー上で変更したファイルは黙って上書きされず、コンフリクトに変換されます。設定する File Locking トグルはありません。

### バージョン履歴

サーバー側のバージョンは組み込みの **Versions** アプリ（アプリ ID `files_versions`）が提供します。標準的な Nextcloud インストールでは**既定で有効**のため、通常は何もする必要がありません。

- 無効化されている場合は再有効化します: **Apps → Versions → Enable**、または：
  ```bash
  sudo -u www-data php /var/www/nextcloud/occ app:enable files_versions
  ```
- バージョンはファイルが変化するたびに自動的に作成されます。Obsidian の **Show version history** コマンドで閲覧・復元できます。
- 過去のリビジョンが存在するには、サーバー上でそのノートが少なくとも一度は変更されている必要があります。保持世代は管理者がサーバー側で設定します（`config.php` の `versions_retention_obligation`）。

### その他、確認しておく価値のある設定

これらは厳密には必須ではありませんが、セルフホスト環境ではスムーズで信頼できる同期のためにしばしば注意が必要になります。いずれもサーバー側（管理者）の設定です。

- **信頼済みドメイン（Trusted domains）** — 接続先のホストが `config.php` の `trusted_domains` に列挙されている必要があります。さもないとサーバーがリクエストを拒否します。必要に応じてドメイン／IP を追加してください。
- **HTTPS とリバースプロキシ（Login Flow v2 で重要）** — リバースプロキシの背後では、`overwriteprotocol => 'https'`、`overwritehost`、`overwrite.cli.url`、`trusted_proxies` を正しく設定してください。これらが誤っていると、Login Flow v2 が返す URL（およびダウンロード）が誤ったスキーム／ホストを指して失敗することがあります。プラグインのサーバー URL は常に `https://` を使ってください。
- **アップロードサイズ上限（チャンクアップロード／大容量添付向け）** — PHP の `upload_max_filesize` と `post_max_size`、および Web サーバーのボディ上限（nginx の `client_max_body_size`、例: `0` か大きい値）を引き上げてください。チャンクアップロードは小さなチャンクを送りますが、最終的な結合や非常に大きなファイルは依然としてこれらの上限に達します。
- **リクエストタイムアウト** — 大きな Vault や大きいファイルでは、PHP の `max_execution_time` や php-fpm／Web サーバーのタイムアウト（例: nginx の `fastcgi_read_timeout`）を増やしてください。プラグインのネットワークタイムアウトは 30 秒固定です。
- **ブルートフォース保護** — Nextcloud は単一 IP からの繰り返しリクエストをスロットリングし、特に複数のデバイスが同じネットワークから同期する場合や認証エラーの後に HTTP 429 を返すことがあります。これに遭遇したら、**管理設定 → セキュリティ** でそのネットワークを許可リストに追加するか、`config.php` で `auth.bruteforce.protection.enabled`／IP 許可リストを設定してください。
- **バックグラウンドジョブ（cron）** — Nextcloud が推奨する **Cron** 方式のバックグラウンドジョブを設定し、バージョンのクリーンアップやその他の保守処理が確実に動くようにしてください。
- **アプリパスワードと二要素認証** — メインアカウントのパスワードは決して使わないでください。2FA が有効な場合はアプリパスワードが必須です。Login Flow v2 が自動で発行します。
- **チェックサム（任意・推奨）** — プラグインは変更検出に Nextcloud の `oc:checksums`（SHA-256）を優先し、無い場合は自動的に ETag へフォールバックするため、設定は不要です。Nextcloud の既定のチェックサム対応を有効のままにしておくと、最も正確な検出が得られます。

---

## 設定の既定値

設定画面は意図的に最小限です。表示されるのは Server URL・サインイン・Sync folder・
同期間隔・Wi-Fi のみ・除外フォルダ・config フォルダ同期のトグル・「ログを有効化」
スイッチだけです。それ以外の項目は撤去され、固定値またはプラットフォームから自動導出
される値になりました。プラグインの挙動が常に分かるよう、ここに明記します。

### 編集可能な設定（初期値）

変更できる項目です。多くは両プラットフォームで同じ初期値ですが、一部は初回のみ異なります。

| 設定 | デスクトップ | モバイル |
|---|---|---|
| Server URL / Username / App password | 空 | 空 |
| 同期間隔 | 15 分 | 15 分（無効 — *Sync now* または *起動時同期* を使用） |
| Wi-Fi のみで同期 | OFF | ON |
| 設定フォルダを同期（master） | OFF | OFF |
| └ Bookmarks / その他の設定 | ON / ON | ON / ON |
| 自動マージファイルの拡張子 | md, txt, cpp, py, c, h, hpp, rs, go, ts, js, java, sh | 同左 |
| 自動マージファイルの戦略 | Merge | Merge |
| その他ファイルの戦略 | Latest modified | Latest modified |
| ログを有効化 | OFF | OFF |
| 除外フォルダ | 空 | 空 |

> 設定フォルダのカテゴリトグル（Bookmarks・その他の設定）は、master の *設定フォルダを同期* を ON にして初めて効きます。

### 固定値（全プラットフォーム共通）

| 設定 | 値 |
|---|---|
| ネットワークタイムアウト | 30 秒 |
| 起動時同期の遅延 | 1 秒（`0` = 起動時同期しない） |
| チャンク閾値 | 50 MB（デスクトップ）/ 20 MB（モバイル） |
| チャンクアップロード | ON |
| バルクアップロード | ON |
| ファイルロック | OFF（`If-Match` 条件がロスト・アップデートを防止） |
| 最大コンフリクト領域数 | 0（領域数によるフォールバックなし） |
| Compare with remote | ON（デスクトップ・モバイル） |
| ログフォルダ | Vault ルート |
| デバイス名 | 自動（`<platform>-<deviceId>`） |

### プラットフォーム別の自動導出値

| 設定 | デスクトップ | モバイル |
|---|---|---|
| ファイル変更時同期 | ON | OFF |
| 最大ファイルサイズ | 無制限（`0`） | 20 MB |
| ネットワーク同時実行数 | RAM から自動（8GB+ で約 16） | 約 3 |

---

## 仕組み（概要）

接続時、プラグインは `/status.php`（メンテナンスモード）と `/ocs/v1.php/cloud/capabilities` を探索し、サーバーのバージョンと、どの機能（`checksums`、`files locking`、…）が利用可能かを把握します。その後、**端末ごとの状態データベース** — 最後に成功した同期時点での全ファイルのパス・内容ハッシュ・リモートファイル ID のスナップショット — を維持します。各同期はその時点の状態をこのスナップショットおよびサーバーの `sync-token` と差分比較し、変更分のみを転送します。Nextcloud 固有のあらゆる挙動は capability 検出の背後にゲートされるため、同じプラグインがフル機能の Nextcloud Hub でも素の WebDAV サーバーでも同様に動作します（**Progressive Enhancement**）。

---

## テストと信頼性

同期の正しさは広範な自動テストで担保しています：**数百件の高速な純ロジックテスト**（変更のたびに実行）に加え、**実 Nextcloud サーバーに対して 2 端末を実際に動かす E2E テスト群**（コンフリクト解決のオプション全組合せマトリクスやマルチデバイス収束を含む）を備えています。

これらのテストは、同期不整合状態 — **データ消失、無限に続く再アップロード／再ダウンロード、リモートの変更がローカルに反映されない、ローカルの変更がリモートに同期されない** — を防ぐために用意しています。とはいえ、あらゆるケースを完全に網羅できるとは限らず、意図しない動作の可能性を完全に否定することはできません。**そのような現象を見つけたら、ぜひ気軽に [issue](https://github.com/siosig/obsidian-nextcloudsync/issues) に投げてください。できる限り迅速に対応します。**

---

## プライバシーとセキュリティ

- **このプラグインはテレメトリーを一切収集しません。** 利用データ・解析・クラッシュレポートはどこにも収集・送信されません。発生する唯一のネットワーク通信は、あなたの Vault とあなた自身の Nextcloud/WebDAV サーバーとの間の同期だけです。
- アプリパスワード／認証情報は Obsidian の **secret credentials ストア**に保管され、`data.json` に平文で書かれることはありません。
- **あなたのメインアカウントのパスワードは使用も保存もされません** — 使われるのはアプリパスワード（手動発行または Login Flow v2 経由）のみです。
- すべてのネットワーク通信は Obsidian の `requestUrl` API を使用します。
- Obsidian の設定フォルダ（`.obsidian/`）は既定で同期から除外されます — 同期されるのはあなたのノートとその他の Vault ファイルのみです。**設定フォルダを同期**（下記参照）で、その一部を選択的に同期するようオプトインできます。**コミュニティプラグイン（`.obsidian/plugins/`）とプラグイン自身の同期状態データベースは、設定にかかわらず決して同期されません** — これらは実行可能コードと端末固有の状態を保持しており、端末間で上書きするのは安全ではないためです。

---

## 制約

- **エンドツーエンド暗号化（E2EE）** はHTTPSレイヤの暗号化に依存しており、プラグイン自体が重複して暗号化するロジックは施していません。これは最大限の通信安全性とパフォーマンスのトレードオフに基づいた設計です。
- **設定フォルダの同期はオプトインです。** 設定で **設定フォルダを同期** を有効にすると、`.obsidian/` の設定を 2 つのトグル — **Bookmarks** と **その他の設定**（外観、テーマとスニペット、ホットキー、コアプラグイン設定）— で選んで端末間で同期できます。**コミュニティプラグインとプラグイン自身の同期状態データベースは決して同期されません**（実行可能コード／端末固有の状態のため）。コアプラグイン設定の同期された変更を反映させるには、もう一方の端末で Obsidian の再起動が必要な場合があります。
- 主に Markdown／テキストの Vault 向けに設計されています。単一ファイルで数百 MB 級のものは v1 の設計目標を超えます。
- Vault はローカルストレージに置いてください — 別のクラウド同期（例: iCloud Drive）と同時に二重管理しないでください。
- Nextcloud 固有の機能には互換性のあるサーバーバージョンが必要です。古いサーバーや非 Nextcloud のサーバーでは、透過的にコア WebDAV 同期へフォールバックします。

---

## コントリビュートとフィードバック

Issue・Pull Request を [GitHub](https://github.com/siosig/obsidian-nextcloudsync) で歓迎します。プラグインはまだ成熟途上のため、どんな種類のフィードバックも特に貴重です：

- 🐛 **バグを報告する** → [GitHub Issues](https://github.com/siosig/obsidian-nextcloudsync/issues)
- 🙋‍♂️ **機能を要望する／感想を共有する** → [GitHub Discussions](https://github.com/siosig/obsidian-nextcloudsync/discussions)

---

## ライセンス

[MIT](LICENSE) © Daisuke ITO
