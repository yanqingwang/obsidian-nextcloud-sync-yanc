# 変更履歴

**Nextcloud Sync for Obsidian** の主要な変更点をまとめています。

フォーマットは [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) に準拠し、
バージョニングは [セマンティック バージョニング](https://semver.org/spec/v2.0.0.html) に従います。

掲載は安定版（`x.y.z`）のみです。プレリリースの `-beta.N` ビルドは GitHub の
[Pre-release](https://github.com/siosig/obsidian-nextcloudsync/releases) として公開され、
内容は次の安定版エントリにまとめられます。

> 英語版（原文）は [`CHANGELOG.md`](CHANGELOG.md) を参照してください。

## [1.0.1] - 2026-08-29

### 修正
- Obsidian コミュニティストアの審査警告を解消し、自動リスト登録チェックを通過するようにしました：
  - 認識される単一著作権の `MIT` ライセンスを採用（上流の帰属は新しい `NOTICE` ファイルに保持）。
  - `js-yaml` 依存を削除。テスト用モックは `yaml` パッケージを使用するようになり、本番コードは引き続き Obsidian 組み込みの `parseYaml`/`stringifyYaml` を利用します（`js-yaml` がバンドルされることはありませんでした）。
  - `PluginSettingTab.getSettingDefinitions()`（宣言型設定 API）を実装し、Obsidian 1.13.0 以降の設定検索で全設定が検索可能になりました。1.13.0 未満向けのフォールバックとして `display()`/`render()` を残しているため、1.11.4–1.12.x での動作は変わりません。

## [1.0.0] - 2026-08-29

[obsidian-nextcloudsync](https://github.com/siosig/obsidian-nextcloudsync) v0.7.43 からのフォーク（「YANC」フォーク）です。

### 追加
- 隠しファイル（名前が `.` で始まる）の既定での除外（`.env`、`.gitignore`、`.DS_Store` など）。
- すべてのドットフォルダ（いずれかのパス要素が `.` で始まる）の既定での除外（`.git`、`.hidden`、`.obsidian` など）。
- 設定内の独立した無効化トグル 2 つ：**Exclude hidden files** と **Exclude dotfolders**。

## [0.7.43] - 2026-08-27

### 変更
- 同期エンジンの内部構造を整理しました。ユーザーから見える変更はありません。エンジンを責務ごとのモジュール（判定・走査・セッション記録・転送・コンフリクト適用・削除・解決・ディレクトリ・watch 操作・ミラー）に分割し、残った判定処理をファイル操作・通信から切り離しています。同期・コンフリクト解決・削除・フォルダ処理・Mirror from remote のいずれも従来どおりに動作します。
- この整理で得られたのはテスト容易性です。テスト件数は 992 → 1,325 に増え、異常なサーバ応答（途中で切れた一覧・欠けたプロパティ・想定外のチェックサム形式など）を、実際の Nextcloud に接続せずに直接検証できるようになりました。切り出した箇所はすべて、意図的に壊してテストが検出することを確認しています。

## [0.7.42] - 2026-08-27

### 修正
- Nextcloud ではない WebDAV サーバが、依然として Nextcloud として扱われていました。その場合は Nextcloud 専用機能を無効化して標準 WebDAV にフォールバックするとドキュメントに明記していましたが、実際のフォールバックは「接続そのものに失敗したとき」しか働かず、判定に使う probe は普通の WebDAV サーバに対して失敗せず応答してしまいます。サーバ種別を probe の応答内容から判定するようにしたため、そうしたサーバには標準クライアント（素の WebDAV サーバが受け付けるフォルダ一覧取得を含む）が選ばれます。Nextcloud への接続は変わりません。
- メンテナンス中のサーバが、そう伝えられませんでした。サーバ種別の判定中にメンテナンスの通知が握りつぶされ、一時的な状態が一般的な `HTTP 503` エラーとして表示されていました。

## [0.7.41] - 2026-08-23

### 修正
- Android でブラウザ経由のログインが完了しませんでした（[#34](https://github.com/siosig/obsidian-nextcloudsync/issues/34)）。ブラウザで承認する間 Obsidian が背面に回ると OS がタイマーを停止するため、認可済み資格情報のポーリングが止まり、ログインが無言のまま進まなくなっていました。アプリが前面に戻った時点でポーリングを再開し、サーバが許可する 20 分間は試行を続けます。
- Obsidian に戻った直後に同期がまれに失敗しました。背面から復帰した直後の最初の読み取り系 WebDAV リクエストが、接続自体は正常でもタイムアウトすることがありました。読み取り系は一過性のタイムアウト時に自動リトライし、同期失敗として表面化しないようにしました。
- プラグイン読み込みのたびに Nextcloud のサーバログにエラーが記録されていました（[#37](https://github.com/siosig/obsidian-nextcloudsync/issues/37)）。Nextcloud のファイルストレージが実装していない `sync-collection` REPORT を発行しており、サーバがその拒否を ERROR としてスタックトレース付きで記録していました。このリクエスト自体を送らないようにしました。同期の動作に影響はなく、サーバのログが静かになるだけです。

## [0.7.40] - 2026-08-18

### Fixed
- 背面（非アクティブ）タブで開いていたノートが、同期によって閉じられることがあった（[#32](https://github.com/siosig/obsidian-nextcloudsync/issues/32)）。0.7.30 で入れたその場更新は、ロード済みビューを通してしか「開いている」と判定できておらず、Obsidian 1.7.2 以降は背面タブがプレースホルダのビューを持つため、該当ノートがディスク上で置き換えられてタブが直前のノートに戻っていた。背面タブについても、ノート・バイナリ添付の双方をその場で更新するようにした。

### Security
- 開発・テスト用の依存パッケージに残っていた脆弱性の指摘を、`extract-zip` をツリーから除去し（`@puppeteer/browsers` 3.x 経由）、`deepmerge-ts` を 8.x に固定することで解消した。いずれもビルド時・テスト時にのみ使うパッケージであり、実行時の依存パッケージは変更なし。

## [0.7.39] - 2026-08-07

### Security
- 開発・テスト用の依存パッケージに残っていた脆弱性の指摘を、`undici` / `ip-address` / `fast-uri` / `brace-expansion` / `diff` を修正済みバージョンへ固定することですべて解消した。いずれもビルド時・テスト時にのみ使うパッケージであり、実行時の依存パッケージは変更なし。リリースされる `main.js` も 0.7.38 とバイト単位で同一のため、動作の変更はない。

## [0.7.38] - 2026-08-01

### Fixed
- iOS で名前に半角スペースを含むファイル・フォルダが同期できなかった（[#25](https://github.com/siosig/obsidian-nextcloudsync/issues/25)）。スペースを含むパスがすべて HTTP 404 になり、対象のノートが一切同期されなかった。0.7.33 で別の問題に対処するため iOS だけ URL エンコードをやめていたが、その判断が誤りだったため、全プラットフォームで同じ方式に統一した。
- Server URL の末尾がスペースや非ラテン文字を含むフォルダ名の場合に、正しくエンコードするようにした。エンコード済みの URL を貼り付けた場合はそのまま扱う。

### Added
- デバッグログに失敗したファイル名がすべて出るようにした。同期が失敗してもログにはエラー件数しか残らず、どのファイルが影響を受けたのか分からなかった。各失敗をパス付きで記録し、通信エラーはどの操作で失敗したか（例: `HTTP 404 (GET)`）も表示する。

## [0.7.37] - 2026-07-30

### Fixed
- 「ファイル変更時に同期」が、サーバの状態を確認しないまま編集後のノートをアップロードしていたため、前回の同期以降に他デバイスが加えた編集が、競合もマージも通知もないまま置き換わっていた（[#23](https://github.com/siosig/obsidian-nextcloudsync/issues/23)）。この経路でも対象ファイルのリモート状態を取得し、通常の同期と同じ競合解決を通すようにした（Markdown はマージされ両方の変更が残る）。ローカルでの削除も、他デバイスが直前に編集していたファイルは削除せず復元する。
- アップロード直後に同じファイルを再ダウンロードしていた。同期状態にアップロード前のリモート識別子を記録していたため、次の同期で必ず「サーバ側が変わった」と判定していた。

### Added
- 競合が自動解決されたことを通知するようにした。通常のアップロード・ダウンロードでは無音のままだが、マージ・競合マーカーの書き込み・片側採用による解決は知らせる。

## [0.7.36] - 2026-07-27

### Security
- `js-yaml` を 5.2.2 へ更新し、[GHSA-pm4m-ph32-ghv5](https://github.com/nodeca/js-yaml/security/advisories/GHSA-pm4m-ph32-ghv5)（フローコレクションの指数時間パース）を修正。
- 推移的依存の `fast-uri` をパッチ版へ固定し、[GHSA-4c8g-83qw-93j6](https://github.com/advisories/GHSA-4c8g-83qw-93j6) と [GHSA-v2hh-gcrm-f6hx](https://github.com/advisories/GHSA-v2hh-gcrm-f6hx)（ホスト混同）を修正。
- 推移的依存の `fast-xml-parser` をパッチ版へ固定し、[GHSA-8r6m-32jq-jx6q](https://github.com/advisories/GHSA-8r6m-32jq-jx6q)（DOCTYPE 重複宣言によるエンティティ展開上限のリセット）を修正。
- 推移的依存の `tar` をパッチ版へ固定し、[GHSA-w8wr-v893-vjvp](https://github.com/advisories/GHSA-w8wr-v893-vjvp)（PAX 数値パスの型混同によるプロセスクラッシュ）を修正。
- いずれもテスト・lint ツール専用の devDependency でプラグイン本体には同梱されず、ユーザーに見える挙動の変更はありません。

## [0.7.35] - 2026-07-27

### Fixed
- **2台の端末に存在するノートが無言で上書きされる問題を修正** — ローカルとサーバの両方にファイルが存在するのに同期記録が無い場合（例：同じ名前のノートを2台で別々に作成した場合）、リモートだけの変更とみなしてダウンロードでローカルを上書きし、競合として報告しないままローカルの編集内容が失われていました。今後はこうしたファイルも正式な競合として解決します（Markdown はマージされ、両方の内容が残ります）。これは初回同期と同じ扱いです。両方の内容が完全に同一の場合は転送せず同期済みとして記録するだけになります（[#23](https://github.com/siosig/obsidian-nextcloudsync/issues/23)）。
- **アップロードに失敗したマージ結果が取り残される問題を修正** — マージ結果の送信中に通信が切れると、次回の同期がそのファイルを収束済みと誤読して再送しないため、マージ結果がその端末に留まり他の端末へ届かないことがありました。次回の同期で再送されるようになりました。

## [0.7.34] - 2026-07-22

### Security
- `js-yaml`を5.2.1へ更新し、[GHSA-724g-mxrg-4qvm](https://github.com/nodeca/js-yaml/security/advisories/GHSA-724g-mxrg-4qvm)（`!!omap`による二次関数的複雑度DoS）を修正。
- 3つの異なる`minimatch`メジャー系統が引き込んでいた推移的依存の`brace-expansion`をそれぞれのパッチ版へ固定し、[GHSA-3jxr-9vmj-r5cp](https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-3jxr-9vmj-r5cp)（指数時間DoS）を修正。
- いずれもテスト・lintツール専用のdevDependencyでプラグイン本体には同梱されず、ユーザーに見える挙動の変更はありません。

## [0.7.33] - 2026-07-18

### Fixed
- iOS でリモート URL が二重エンコードされる問題を修正 — iOS の WebDAV クライアントが、`requestUrl` が内部で既にエンコードしているパスをさらに percent-encode しており、スペースなど特殊文字を含む URL が壊れていました。iOS ではこのエンコードをスキップし、デスクトップ・Android の挙動は変更していません。
- Sync status：「Recent activity」一覧の冗長表示を解消 — run 区切り行（`— sync HH:MM —`）は、そのrunで複数ファイルが同期された場合のみ表示するようにしました。1ファイルだけのrunでは、区切り行と各行の両方に同じ時刻が重複して表示されていた問題を解消しました。

### Changed
- 内部整備：lint ゲートを Obsidian レビュアーへ再同期 — Obsidian コミュニティディレクトリの自動レビューが使う版に `eslint-plugin-obsidianmd` を追随させ、それに伴う `createEl` ショートカット関連の警告を解消しました。ユーザーに見える挙動の変更はありません。

## [0.7.32] - 2026-07-16

### Added
- **リボンに同期ボタンを追加** — 「Sync with Nextcloud」リボンアイコンをワンクリックで手動同期を実行できるようになりました。モバイルでは Obsidian がリボンアイコンをハンバーガーメニュー内に表示するため、コマンドパレットやプラグイン設定を開かずに同期を開始できます（[#19](https://github.com/siosig/obsidian-nextcloudsync/issues/19)）。動作は既存の「Sync now」と完全に同一で、コマンドや設定画面のボタンは変更していません。

## [0.7.31] - 2026-07-14

### Added
- Sync status ダイアログから Mirror from remote を実行できるようになりました。**Sync now** の隣に **Mirror from remote** ボタンを追加し、設定 → Maintenance の既存ボタンと同じ確認（ダウンロード予定数・削除予定数）が開き、まったく同じ処理を実行します。より便利な 2 つ目の入り口を増やしただけで、設定側のボタンはそのまま残っています。

### Changed
- 内部改善：マージエンジンのコーディング規約を厳格化しました（ユーザーから見える挙動変更はありません）。`any` 型や紛れ込んだ `console.log` 呼び出しを、手動レビューではなく lint ルールで機械的にブロックし、マージ戦略の依存先の一つを CommonJS の require から標準的な ES モジュールの import に切り替えました（フルテストスイート・実 Nextcloud サーバでの検証を含む）。

## [0.7.30] - 2026-07-12

### Fixed
- バックグラウンド同期で編集中のノートが閉じられなくなりました。自動同期または「今すぐ同期」が、開いているファイルへリモート側の変更を適用すると、そのノートの表示が消えて空のペインになり、再度開き直す必要がありましたが、現在開いているファイルは表示を保ったまま内容だけがその場で更新されるようになりました。

## [0.7.29] - 2026-07-09

### Added
- 大量削除の安全ブレーカーが、スキップされた全パスを（件数だけでなく）省略なく記録するように。
- Sync status ダイアログで mass-delete breaker のエラー行をクリックすると、スキップされた全パスを省略なく一覧表示するレポートノートが開くように（従来のキャップ付きインライン表示は廃止）。
- ディレクトリ側の mass-delete breaker に、「Use remote」「Use local」のどちらか一つの操作でスキップされた全ディレクトリを一括解決できる機能を追加。

### Fixed
- mass-delete breaker のエラー行クリックが、誤ってエラーラベルと同じ名前の空ノートを作成してしまうことがあった不具合を修正。今はレポートノートを開く。

## [0.7.28] - 2026-07-08

### Fixed
- 前バージョンの自動レビューが失敗し、コミュニティプラグインディレクトリから外れていた不具合を修正。Obsidian の審査は UI 文言の `sentence-case` ルールを無効化することを許可しておらず、リテラルなフォルダ名 `.git` に対する誤検知を回避するためルール抑制コメントを使っていたのが原因でした。抑制ではなく実際の原因を修正: ルールのブランド一覧が "git" をどこに出現しても "Git" に強制変換していましたが、これは常に小文字が正しい実在のファイルシステムのフォルダ名に対しては誤りだったため、ブランド一覧の上書きから "Git" を除外しました。ユーザー向けの挙動変更はありません。

## [0.7.27] - 2026-07-07

### Fixed
- `.git` と `.trash` フォルダを同期しないように。Obsidian のローカルゴミ箱フォルダ（`.trash`）とリポジトリの `.git` フォルダが、これまで全デバイスへアップロード／ダウンロードされていました。`.trash` の同期は各デバイスのゴミ箱を散らかし、プラグイン自身の「ゴミ箱へ移動して削除」挙動と競合して churn を起こします。`.git` をファイル単位で同期するとリポジトリが破損し得ます。両者を *Excluded folders* リストに関わらず**常に除外**するようにしました（設定画面が以前から「除外済み」と説明していた挙動に実装を合わせた形です）。これは対象を絞った除外で、vault ルート直下のそれ以外の dot フォルダ（例: `.archive/`）は従来どおり同期します。既にサーバーへ上がっている `.trash`/`.git` は削除せずそのまま残します（不要ならサーバー側で手動削除してください）。

## [0.7.26] - 2026-07-06

### Fixed
- 「A sync is already in progress」で永久に詰まる不具合を修正。最初のステップ（サーバー接続と capabilities 取得）が失敗するとエンジンが「同期実行中」フラグを立てたままエラーを握り潰し、以降すべての同期が弾かれ、起動時同期が同じ失敗を再誘発するため再起動しても直りませんでした。この接続ステップをエラーハンドリングの内側へ移し、失敗が表面化して次回試行で回復するようにしました。
- **Network timeout** 設定が実際のリクエストに適用されるように。従来は一切配線されておらず、接続だけ受け付けて応答しないサーバーで同期が無限にハングし得ました（上記ロックも誘発）。全 WebDAV リクエストが設定タイムアウト（既定 30 秒・0=無制限）で上限付けられ、詰まったリクエストは失敗して次回同期でリトライします。
- 全ソース監査で、1 ステップの失敗が内容を無音で喪失／復活させ得るデータ安全性経路を一括修正：クリーンマージの upload 失敗時もフラグ維持／リモート削除が成功したときだけ追跡解除／中断されたアトミック書き込みで新旧両方を失わない／大容量（チャンク）アップロードも小ファイルと同じ楽観的排他を守る／同期中クラッシュ後はディスク状態から復旧／空 base マージが2独立編集を1行に融合しない。加えて force-resolve / restore の二重クリックガード、モバイル watch モードの実行時無効化。

## [0.7.25] - 2026-07-06

### Fixed
- クライアント側キャッシュに起因する同期の恒久的な詰まりを修正しました。アプリのネットワークキャッシュが、あるファイルの不完全な異常レスポンスを一度でも保持すると、そのファイルは再起動や「Sync now」「Use remote」を何度試しても永久に同期に失敗し続けることがありました。全リクエストでこのキャッシュを明示的に無効化したため、異常なレスポンスが再利用されることはなくなり、次回同期では必ずサーバーへ新規問い合わせして正しい結果を得られます。

### Changed
- トラブルシューティング用ログを「見つかる 1 ファイル」に再設計しました。**Debug → Enable logging** を ON にすると、端末ごとに単一のファイル `nextcloud-debug_<device>.txt` を Vault ルートに出力します（従来の 2 ファイルを統合）。ON にした瞬間に書き込み先のファイル名を明示する通知が出て、Obsidian は **設定 → ファイルとリンク → すべてのファイル拡張子を認識** が ON でないと `.txt` を隠すことも案内します（OS や Nextcloud 上でも開けます）。ログの書き込みに失敗した場合も無言ではなく通知が出るようになりました。

## [0.7.24] - 2026-07-05

### Added
- フロントマターが本文から独立した競合戦略を持つようになりました。`.md` ファイルのフロントマターブロックと本文を別々に解決し、新設の **Frontmatter strategy** 設定（既定: merge）がフロントマターを、既存のファイル種別マージ戦略が本文を担当します。
- 2段階の競合解決：一次戦略（frontmatter / auto-merge / other-file）だけでは衝突を完全に解決できない場合、残った衝突領域・フィールドは新設の明示的な **Conflict strategy**（既定: conflict markers）にフォールバックし、all-or-nothing の判定ではなくなりました。
- **Mirror from remote** がクリック直後に進捗ダイアログを開き、各段階（リモート読み込み・比較、続いてダウンロード/削除）をリアルタイム表示するようになり、固まって見える問題を解消しました。
- mass-delete 安全上限を新設の **Advanced (use with caution)** セクションで設定可能にしました（`-1` = 自動 / `0` = 無制限 / 固定値）。

## [0.7.23] - 2026-07-04

### Fixed
- モバイルの **Sync interval** スライダーが実態どおりの表示になりました。モバイルでは OS が背景タイマーを止めるため定期同期はもともと発火しませんが、スライダーはグレーアウト状態でもデスクトップ既定の 15 分を表示していて、「15分ごとに同期している」ように見えていました。モバイルの新規インストールでは初期値を 0（手動のみ）にし、無効スライダーの表示が実挙動と一致するようにしました。デスクトップは変更なし。既に設定済みの間隔値もそのまま保持します。

## [0.7.22] - 2026-07-04

### Added
- ウォッチモードでフォルダの変更も即時伝播するようになりました。**ファイル変更時の同期** を ON にしていると、フォルダの作成・削除・リネームがサーバーへ即時反映されます（作成→フォルダを作成、削除→ゴミ箱へ、リネーム→移動）。従来ファイルだけだった即時伝播がフォルダにも揃います。伝播中はステータスバーに表示され、設定の説明文も「対象はファイル**とフォルダ**、作成・編集・削除・リネームすべて（デスクトップ限定）」と実態どおりに修正しました。
- **設定 → Maintenance** に新しい **「リモートの状態に合わせる（Mirror from remote）」** アクションを追加しました。この端末の Vault をリモートの現在状態に完全に一致させます。リモートにあるものをすべてダウンロードし、リモートに無くなったローカルのファイル／フォルダを Obsidian のゴミ箱へ移動します（復元可能）。実行前に「ダウンロード予定数／削除予定数」を確認ダイアログで提示し、ローカルの未同期の変更は破棄されます。大量削除の安全上限はバイパスします（あなたが明示的にリモートを正と宣言したため）が、リモート一覧を完全に取得できない場合は何も削除せず中止します。他の同期ツールから移行した端末をリモートに従わせたいケースに便利です。

## [0.7.21] - 2026-07-04

### 追加
- Sync status ダイアログに一括強制解決を追加しました。ドロップダウンを1件ずつ操作する代わりに、戦略（Use remote / Use local / Latest modified / Biggest size）を一度選ぶだけでリスト中の全競合にまとめて適用できます。同値（更新時刻・サイズが等しい）は手を付けず、失敗したものはコンフリクトのまま残すので、黙って失われることはありません。

### 修正
- 競合時の frontmatter（YAML プロパティ）を、テキストではなく構造化データとして解決するようにしました。ノートの `---` ブロックの中に競合マーカー（`<<<<<<<` / `>>>>>>>`）が入り込むこと（Obsidian のプロパティが壊れ、次の同期でさらに入れ子になる原因）がなくなりました。リスト項目（tags・aliases・related）は本物の 3-way 集合マージになり、片方の端末で削除したタグが復活せずきちんと消え、表記ゆれ（`#tag` と `tag`）は1つにまとまり、サーバー側ツールによる外部からの変更も正しく伝播します。
- 「Use remote」「Use local」「Latest modified」「Biggest size」の強制解決が本物のマーカーなし版を復元するようになりました。テキスト競合でノートにマージマーカーが書き込まれた場合、これまで Sync status ダイアログからの強制解決はマーカー入りの内容を再同期するだけで警告だけが消えていました。今回から競合時に両方の clean 版をスナップショットし、ノートが実際に綺麗になったときにだけ競合を解除します。

## [0.7.20] - 2026-07-02

### 修正
- コンフリクトマーカーの消し忘れ（手動解決で残った `<<<<<<< LOCAL` または `>>>>>>> REMOTE` 単体の行）が原因のデッドロックを解消しました。従来この状態のファイルは push されないまま保留され、毎回の同期でコンフリクトに戻り続けていました。今後は通常のマージを通って収束し、ローカル・リモート双方から余分なマーカーを取り除きます（self-heal）。完全なマーカー集合や入れ子マーカーは従来どおり保留します。

### 追加
- Sync status ダイアログに per-file の強制解決を追加しました。各コンフリクトファイルにドロップダウン（Use remote / Use local / Latest modified / Biggest size）と Apply ボタンが付き、その場で即座に解決できます。同値（更新時刻・サイズが等しい）の場合は何もせず、失敗時はコンフリクト状態を維持します。

## [0.7.19] - 2026-07-01

### 変更
- フロントマターのマージがセマンティックになりました。`tags`・`aliases`・`related` などの配列フィールドは端末をまたいでユニオンマージ（重複排除）され、`title`・`status` などのスカラーフィールドは 3-way 比較で解決されます。片方だけの変更は自動解決し、真のコンフリクトは新しい設定ポリシー（最終更新優先 / リモート優先 / ローカル優先。既定: 最終更新優先）で決定します。フロントマターのないノートは影響を受けません。設定は「Conflict Resolution」セクションの「Frontmatter scalar conflict [experimental]」に追加されています。

## [0.7.18] - 2026-06-30

### 修正
- コンフリクトのマージ精度を向上しました。同じノートを 2 台の端末で編集した際、最後に同期した版を本当の 3-way マージのベースとして使うようになり、自動マージがよりきれいになり編集の取りこぼしも減りました。
- 既存のコンフリクトマーカーが同期のたびに再マージされ、該当ノートが際限なく肥大化していた再入ループを停止しました。
- no-op の「引き分け」がルート ETag の高速経路で必要な再同期を短絡してしまう不具合を修正し、変更を取りこぼす経路を塞ぎました。

## [0.7.17] - 2026-06-30

### 修正
- スマートフォンで、0.7.16 で各設定スライダーの隣に追加した編集可能な数値ボックスが行いっぱいに広がり、スライダーを画面外へ押し出していました。数値ボックスをコンパクトな幅に固定し、数値ボックスとスライダーの両方が見えて操作できるようにしました。

## [0.7.16] - 2026-06-30

### 変更
- コンフリクト解決をファイル種別ごとに選べるようになりました。旧来の3設定（自動マージのトグル・frontmatter コンフリクト戦略・マージ失敗時ポリシー）を、**自動マージファイルの拡張子リスト**＋**自動マージファイルの戦略**＋**その他ファイルの戦略**に集約しました。各戦略は Merge（3-way）/ Latest modified / Biggest size / Local wins / Remote wins のいずれかで、コンフリクトは必ず決着します（保留・エラーのモードはありません）。サイズ/更新時刻が同値の場合は両側不変で次回同期に再評価します。旧設定は自動移行され、不要なキーは除去されます。
- Merge 戦略では、非テキスト（バイナリ）ファイルにコンフリクトマーカーを書き込む代わりに一切触らずフラグのみ立てるようになりました。また、内容を重複させてしまうマージ（空 base 由来のまれな reconcile 不具合）を、破損結果を書かずコンフリクト扱いへ降格する膨張ガードを追加しました。

### 追加
- 各数値設定のスライダーの隣に編集可能な数値入力ボックスを追加し、キーボードで正確な値を入力できるようにしました。範囲外の値はクランプ、不正な入力は直前の値へ復帰し、入力ボックスとスライダーは常に同期します。

## [0.7.15] - 2026-06-29

### 変更
- 「最大ファイルサイズ」設定がアップロードだけでなくダウンロードにも適用されるようになりました。上限を超えるリモートファイルは取得前にスキップされます（サーバが PROPFIND の `getcontentlength` で申告するサイズを正典として判定）。これにより巨大なノートがモバイルでメモリに読み込まれてアプリをクラッシュさせることがなくなります（issue #8）。スキップは非破壊的かつ自己修復的で、上限を引き上げれば次回の同期で通常どおりダウンロードされます。

## [0.7.14] - 2026-06-27

### 修正
- モバイルでスライダー値が省略されていた問題を修正: 画面幅の狭い端末で "15" や "30" などの 2 桁以上の値が ".." と切り捨てられていました。値ラベルに `flex-shrink: 0; white-space: nowrap` を追加し、どの画面サイズでも完全に表示されるようになりました。

## [0.7.13] - 2026-06-27

### 変更
- **起動時同期の操作を簡素化＋スライダー範囲を整理**：独立していた *Sync on startup* トグルを *Startup sync delay* スライダー 1 本に統合しました（`0`＝起動時同期しない、`1`〜`10` 秒＝遅延秒数）。数値スライダーの範囲・刻みも見直し、*Sync interval* は 4 分刻み（0〜60、0＝手動のみ）、*Network concurrency* は 0〜60 の 4 刻み（0 は 1 として動作）になりました。

### マイグレーション
- 既存の設定はアップグレード時に自動で引き継がれ、起動時同期を OFF にしていたインストールは OFF のまま維持されます（遅延が 0 に収束）。

## [0.7.12] - 2026-06-27

### 変更
- **利用価値の少ない 5 設定を UI から撤去し固定値化**：ファイルロックは常に OFF（常時有効の `If-Match` プリコンディションが LOCK/UNLOCK の往復なしでロストアップデートを防止）、チャンクアップロードは常に ON、チャンク閾値はプラットフォーム別の固定値（デスクトップ 50 MB / モバイル 20 MB）、最大コンフリクト領域数は常に無制限、*Compare with remote* はエクスプローラメニューとコマンドで常時利用可能になりました。

### マイグレーション
- これら 5 設定に以前保存していたカスタム値は、アップグレード時に自動でクリーンアップされ、全インストールが同一の単一経路に揃います。

## [0.7.11] - 2026-06-26

### 変更
- **Debug 設定を単一トグルに縮小**：Device name と Log folder の入力欄を撤去し、*Enable logging (troubleshooting)* のみを残しました。デバイス名は自動導出され、ログは常に vault root に出力されます（sync ログは全操作を記録、debug ログは verbose）。

### マイグレーション
- 以前に設定したカスタムのデバイス名・ログフォルダは、アップグレード時に自動／vault root の既定値へリセットされ、全インストールが同一経路に揃います。過去のカスタムフォルダに既に書かれたログファイルはそのまま残します。

## [0.7.10] - 2026-06-26

### 追加
- **除外フォルダ**のユーザー設定：同期をスキップするフォルダパスを指定可能に
- 除外フォルダ追加時のインラインオートコンプリートサジェスト（Vault 内フォルダ名から補完）
- 自動マージ失敗時の動作（コンフリクトポリシー）を選択可能に：`mark`（コンフリクトマーカー挿入）/ `keep-local` / `keep-remote` / `revert` から選択
- モバイルで **Compare with remote** コマンドが利用可能に（縦積み diff 表示）
- モバイル向け：起動時同期のオン/オフ設定を追加

### 変更
- 設定ファイル同期を **Bookmarks** と **Others** の 2 カテゴリに整理
- 全同期設定が設定 UI でユーザー編集可能に。`DEFAULT_SETTINGS` を唯一の真実源とし、新しい **Debug** セクションでデバイス名・ログフォルダ・ログ有効化をまとめて管理

## [0.7.9] - 2026-06-25

- **修正: 非常に長い名前のノートが Android に同期されない問題** — ファイル名自体はファイルシステムの「1 コンポーネント 255 バイト」上限内なのに、アトミック書き込みで使う一時ファイルが 18 バイトの接尾辞を付けることで一時ファイル名が上限を超え、`FILE_NOTCREATED` エラーでダウンロードに失敗していました（日本語は 1 文字 3 バイトのため、タイトルが約 80 文字で発生）。一時ファイルを「対象と同じディレクトリ内の短い固定長ハッシュ名」に変更し、その長さが最終ファイル名に依存しないようにしたため、ファイル名自体が上限内なら確実に書き込めます（rename もアトミックを維持）。OS 的に回避不能な「名前そのものが 255 バイト超」のケースは、不透明なエラーではなく「ファイル名が長すぎます（N バイト / 上限 255）」という分かりやすいメッセージで表示します。

## [0.7.8] - 2026-06-23

- **修正: モバイルでダウンロードが拒否されるリグレッション** — 0.7.7 で追加したダウンロード安全ガードが、受信 `arrayBuffer.byteLength` とサーバ申告 `getcontentlength` の不一致を一律に拒否していた。iOS では Obsidian の `requestUrl` が返すバイト長がサーバの content-length と正当に食い違う（実サーバで検証: PROPFIND==GET Content-Length==実バイトは整合、クライアント側の数値のみマルチバイトで差が拡大）ため、正常なダウンロードが拒否され remote→local 同期が停止していた。ガードを「非空申告に対し本文が完全な空(0 バイト)の場合のみ拒否」に限定。書き込み後検証（書いた長さの自己照合）は影響なし。
- **修正: モバイルでの Sync Status フィルタの可読性** — ステータス別フィルタを、詰まって重なるチェックボックスから、選択状態が明確なフル幅チップ表示に変更。

## [0.7.7] - 2026-06-23

- **マルチデバイスのデータ安全性を強化** — 端末間の稀なエッジケースで変更が消失/取り残される問題を複数修正：(1) 別端末が削除したフォルダ配下で編集・新規作成したファイルが、HTTP 404 で繰り返し失敗せず確実に同期される（「作成済みディレクトリ」キャッシュの stale 化が親フォルダ再作成を阻害していた問題を修正）。(2) 大量削除の安全ブレーキがエラーを記録するようになり、無変更高速化経路が無効化されて「再同期でリトライ」が実際に再評価される。(3) サーバが「非空」と申告しつつ空/切り詰め本文を返す不具合時はローカルを上書きせず拒否（正当に空化したファイルは従来どおり同期＝誤検知ゼロ）。書き込み後にサイズ検証して切り詰め/破損も検知。
- **自動テストの拡充** — 実サーバ × 2 端末の新 E2E テスト群で、削除・リネーム・同時編集・全コンフリクトポリシー組合せなど多数のシナリオについて、データ消失・無限再同期・片方向の同期漏れが起きないことを検査。

## [0.7.6] - 2026-06-23

- **修正: マルチデバイスでの稀なデータ消失を解消** — 競合が未解決のまま残り（失敗ポリシー「error」）かつリモートに他の変更が無い場合、次回同期で 0.7.5 で導入した「無変更高速化」の経路がローカル版で相手デバイスの編集を無言で上書きしてしまう可能性がありました。高速化経路は、競合・エラー・再試行待ちを残した同期の後では自動的に無効化され、未解決のリモート変更を必ず再検出するようにしました（消失しません）。

## [0.7.5] - 2026-06-23

- **無変更同期の高速化（Nextcloud）** — 前回スキャン以降リモートに変更が無い場合、ルートフォルダの ETag で「無変更」を検知し、重いリモート全件リストを省いてキャッシュ済みの状態を再利用するようにしました。実サーバでの計測では、無変更同期が「2 回の大きなディレクトリリスト取得」から「1 回の極小リクエスト」に減ります。正しさには影響しません（リモートの変更は次回同期で必ず取り込まれ、本最適化は Nextcloud のみ・plain WebDAV は従来どおり）。
- **セキュリティメンテナンス** — ビルド/テスト専用の脆弱な依存を修正しました（配布されるプラグイン本体に変更はありません）。

## [0.7.4] - 2026-06-22

- **依存ライブラリのアップデート** — 開発用依存を最新バージョンへ更新しました：`@types/node` 25→26、`eslint` 10.4→10.5、`typescript-eslint` 8.60→8.61。ユーザーが体感できる動作の変化はありません。

## [0.7.3] - 2026-06-21

- **空ディレクトリが正しく同期されるようになりました** — ディレクトリがファイルと並ぶ第一級エンティティとして扱われます。あるデバイスで作成した空フォルダは、リモートおよびほかのすべてのデバイスへ伝播されるようになりました。あるデバイスで空にして削除したフォルダは、リモートおよびほかのすべてのデバイスからも除去されるようになりました — `2011/` のような空の年フォルダが、配下の全ファイルを削除した後もリモートに残り続けるという長年のバグを修正しています。

## [0.7.2] - 2026-06-21

- **設定画面のツールチップが行のどこをホバーしても表示されるように** — 各行に追加した hover ツールチップが、これまで短い太字のタイトルにしか付いておらず、実際に読む長い説明文をホバーしても何も出ず壊れて見えていました。行全体にラベルを付け、行のどこをホバーしても表示されるようにしました。Server URL の説明には引き続き必要なフル WebDAV エンドポイント（`https://<host>/remote.php/dav/files/<user>/`）を明記し、ホストだけを入力して HTTP 405 で失敗しないように誘導します。サインイン領域は「ブラウザでログイン」を先頭に置き、手動の Username/App password 入力との間に区切りを入れています。
- **コンフリクト領域上限が frontmatter だけでなく本文にも効くように** — 本文のマージ競合も diff3 で領域数を計測するようになり、「最大コンフリクト領域数」を超えた場合に常時自動マージするのではなくコンフリクト失敗ポリシーへ回します。既定（`0`＝無制限）では従来どおり自動マージします。
- **同期ステータスダイアログの時刻を 24 時間表記で統一** — 直近セッションのサマリも各行と同じく `HH:mm`（日跨ぎは日付付き）で表示し、ロケール依存の表記をやめました。

## [0.7.1] - 2026-06-21

- **修正: プラグイン自身のログファイルが同期でエラー/無限再送しないように** — 同期の最中に書き込まれる端末別 debug/sync ログが自分の書き込みと競合し、毎回 "Destination file already exists!" が出ていました。今後はそのログの出力トグルが ON の間だけ同期対象から除外します。
- **frontmatter の競合を黙ってマージせず検出するように** — 3-way マージのバグにより、コンフリクトマーカーを指定していても乖離した YAML frontmatter が黙ってマージされていました。今後は競合した frontmatter を競合解決ポリシーに従って顕在化します。
- **Nextcloud で新規フォルダへの初回アップロードが確実に成功するように** — Nextcloud は親フォルダが未存在のとき標準 WebDAV の 409 ではなく 404 を返します。今後は不足する親フォルダを作成してリトライするため、新端末からの初回同期や深い新規パスへの同期が失敗しなくなりました。
- **サーバへの無駄な往復を削減** — 増分同期用の sync-collection レポートに非対応のサーバでは、一度検出したら以降は毎回試行せず直接フルスキャンに進むようにしました。

## [0.7.0] - 2026-06-20

- **Vault キャッシュ列挙によるモバイル同期の高速化** — ローカルスキャンを、ファイルごとのネイティブファイルシステム呼び出しから Obsidian のメモリ内インデックス（`Vault.getFiles`）読み取りに変更し、モバイルで同期を遅くしていた O(N) のブリッジ往復を排除しました。初回スキャンもチェックサム比較が本当に必要なファイルのみハッシュ計算するようになり、ネットワーク並列度はデスクトップ/モバイル共通（プラットフォーム分岐なし）で端末メモリに応じて自動調整されます。`Vault.getFiles` が取りこぼす dot パスは再列挙して同期対象に保ちます。

## [0.6.1] - 2026-06-20

- **「Reset vault index」保守アクションを追加** — 設定に **Maintenance** セクションを新設し、この端末の同期トラッキングインデックスを初回インストール時の状態に戻すボタンを追加しました（確認ダイアログあり）。次回同期で全件を再スキャンします。Vault やリモートのファイルは一切削除せず、実行中の同期があれば先に中断します。既存の **Last session summary** もこの Maintenance セクションに移動しました。
- **初回同期のプレビュー/承認ステップを廃止** — 初回同期時の「ドライラン」確認モーダルを削除し、初回同期はスキャン後そのまま計画を適用するようになりました。全件再スキャンの承認はリセット操作の確認ダイアログが担います。

## [0.6.0] - 2026-06-20

- **同期が大幅に高速化 — モバイル/Android で止まらなくなりました** — 変更検出を、更新時刻設定（モバイルでは無効）依存から「書き込み直後に観測した stat 署名」での比較に変更し、再同期で Vault 全体を読み直さないように。初回同期の二重ハッシュも解消。転送はメモリ安全なバイト予算付きの並列実行に。ファイルロックは既定 OFF とし `If-Match` 楽観制御で安全性を担保（往復削減・ロストアップデートなし）。大きなサーバー一覧の解析で UI が固まる問題も解消。
- **Sync Status ダイアログをモバイルでも開けるように・Sync 実行単位で表示** — 設定の **Last session summary** ボタンが、デスクトップ／モバイル両方でフル機能の Sync Status ダイアログを開きます。Recent activity は Sync 実行単位でグルーピングされ各区切りに開始時刻を24時間制で表示。ステータスフィルター選択は再起動後も保持されます。

## [0.5.0] - 2026-06-20

- **`.obsidian` 設定フォルダの選択的同期** — 設定の **設定フォルダを同期** を有効にすると、Obsidian 設定フォルダのうち選んだカテゴリ（外観 / テーマ・スニペット / ホットキー / コアプラグイン設定 / ブックマーク）を端末間で同期できます（純正 Sync に倣い各カテゴリ個別トグル）。既定は OFF で、ノートは従来どおり同期されます。**コミュニティプラグイン（`.obsidian/plugins/`）とプラグイン自身の同期状態データベースは一切同期されません**（実行コード・端末固有の状態のため）。設定ファイルの競合は newest-wins（更新時刻が新しい方）で解決し、コンフリクトマーカーで JSON を壊しません。従来の単独設定 **ブックマークを同期** はブックマークカテゴリへ自動移行するため、既存ユーザーは変更なしで動作を維持します。[#1](https://github.com/siosig/obsidian-nextcloudsync/issues/1) を解決。

## [0.4.1] - 2026-06-20

- **ログフォルダが未作成でもログファイルが作られるよう修正** — 指定したログフォルダ（例 `_logs`）がまだ存在しない場合に、端末別の sync／debug ログが黙って一切書き込まれない不具合を修正しました。初回書き込み前に親フォルダを自動作成します。

## [0.4.0] - 2026-06-20

- **同期ステータスダイアログのステータス絞り込み** — ダイアログにステータス別チェックボックス（Uploaded／Downloaded／Deleted／Merged／Conflicted／Local wins／Remote wins／Error）を追加。コンフリクトだけ表示するといった絞り込みができます。
- **ファイルとリモートの比較** — 設定で「リモートと比較（エクスプローラメニュー）」を有効にすると、エクスプローラでファイルを右クリックして、ローカルとリモートの更新日時・チェックサム（一致/不一致バッジ付き）・行 diff を表示するポップアップを開けます。そのまま **push**／**pull** で解決でき、いずれも確認ダイアログを挟みます。
- **ローカルとリモートで内容が異なるファイルが必ず同期されるよう修正** — コンテンツチェックサムを返さないサーバー（ETag フォールバック構成）で、ノートがこの端末とサーバーで異なるのに同期されない不具合を修正。今後は検出して通常のコンフリクト解決で収束させます。
- **端末別ログを .txt 拡張子に変更** — sync／debug ログはプレーンテキストのため、Markdown として描画されて崩れないよう `.txt` 拡張子に変更しました。既存の `.md` ログはそのまま残ります。
- **内部リファクタとドキュメント整合** — 公式 Obsidian 開発者ガイドラインに沿うようコードを整理：vault の監視とエンジン初期化を `onLayoutReady` に遅延、ローカルパスを IO 境界で正規化、モーダルのタイトルをネイティブのタイトルバー表示に統一、diff／エラー表示の配色をテーマ追従に変更。

## [0.3.1] - 2026-06-19

- **メンテナンスリリース（Obsidian 1.11.4 以上が必要）** — 機能変更はありません。0.3.0 の自動レビューで失敗した内部 Lint の問題（説明のない ESLint ディレクティブ）を修正してコミュニティディレクトリへの再掲載に対応し、リポジトリの git フックを再有効化して pre-push ゲートがディレクトリ審査と同じ検査を行うようにしました。

## [0.3.0] - 2026-06-19

- **設定画面を4セクションに再編** — 設定を1本の長いリストではなく **Nextcloud** / **Sync** / **Merge** / **Debug** の見出しごとに整理しました。「Max conflict regions」はスライダー横に数値を表示し、既定値が **0＝無制限**（区間数のサーキットブレーカーが既定でコンフリクトマーカーを強制しなくなりました）になりました。
- **端末別ログ** — 任意のフォルダ（ファジー検索のフォルダ選択 UI。既定は Vault ルート）に出力する2種類のオプトインログ。ファイル名に端末識別子が入るため複数端末で上書きされません。
  - **Sync ログ**（`nextcloud-sync_sync_<端末>.txt`）— 同期ごとに1ブロック追記。ヘッダにプラグインのバージョンと全マージ設定、続けて操作ごとにマーカー・パス・ローカル/リモートのチェックサムとサイズを1行で記録。レベルで*重要イベントのみ*（コンフリクト・マージ・side-wins・エラー）か*全操作*かを選べます。
  - **Debug ログ**（`nextcloud-sync_debug_<端末>.txt`）— 時刻付き診断ログ。詳細度（error / debug / verbose）を選択でき、プラグインのバージョンと全設定値のスナップショットを記録。従来の単一 Vault ルートのデバッグファイルを置き換え、旧「デバッグモード」トグルは自動移行します。
- **コンフリクト件数が減らない不具合を修正** — 一度コンフリクト扱いになったファイルが、その後両側で一致（収束）した場合に次回同期でフラグをクリアするようにし、件数が固着しなくなりました。
- **テレメトリー非収集** — 利用データ・解析・クラッシュレポートは一切収集せず、通信はあなた自身のサーバーとの同期のみです。旧ビルド由来の不要な設定キーも読み込み時に `data.json` から除去します。

## [0.2.10] - 2026-06-17

- **コンフリクト表示の明確化** — 同期ステータスダイアログとサマリーで、**merged**（クリーンな自動マージ完了）と **conflicted**（コンフリクトマーカーがファイルに残存）の2種類を別々にカウント・表示するようになりました。これまでは両者が単一の「conflict」件数にまとめられていたため、対応が必要かどうか判断しにくい状態でした。

## [0.2.9] - 2026-06-16

- **Obsidian の自動ストア審査を通過** — インラインスタイルを CSS クラスへ、UI 文言を sentence case へ修正し、掲載が取り下げられていたコミュニティプラグインの自動チェックをクリアしました。

## [0.2.8] - 2026-06-16

- **より多くの環境でインストール可能に** — 必要な最小 Obsidian バージョンを、実際の最小要件である `1.11.4` に是正しました（従来は事実上、最新版だけを要求していました）。

## [0.2.7] - 2026-06-16

- **手動「Sync now」ボタンと直近24時間のアクティビティ表示** — 同期ステータスダイアログ最上部に **Sync now** ボタンを追加し、スクロール可能な**直近24時間のアクティビティ履歴**（ファイルごとに1行・アイコン先頭のコンパクト表示）を表示。自動同期間隔の変更はリロード不要で即時反映されるようになりました。

## [0.2.6] - 2026-06-11

- **ウォッチモードの自己フィードバックループを修正** — プラグイン自身の書き込み（ダウンロードやマージ結果）をユーザー編集として再検出しなくなり、「ファイル変更時の即時同期」有効時の不要な再アップロード・エラーの連発・意図しないリモート削除を解消しました。
- **1 ファイルの失敗で同期全体が中断しなくなりました** — ファイル単位のエラー（サーバー側 403 など）は記録して残りのファイルの同期を継続します。ネットワークエラーは従来どおりバックオフ付きで再試行キューに積まれます。
- **同期ステータスダイアログにエラー詳細を表示** — ステータスバーをクリックすると、エラー件数だけでなく「前回同期のエラー」一覧（失敗ファイルごとに 1 行、クリックでファイルを開く）が表示されるようになりました。
- **状態データベースの保存を直列化** — ウォッチモードの並行同期が競合して同期状態ファイルが破損・消失する問題を解消しました。

## [0.2.5] - 2026-06-11

- **リモート削除のスコープガード（セキュリティ強化）** — サーバーが報告する削除を同期スコープと照合してから適用するようにし、悪意ある／侵害されたサーバーが同期フォルダ外（`.obsidian/` 配下など）のファイルをクライアントに削除させられないようにしました。

## [0.2.4] - 2026-06-10

- **ダウンロードの堅牢化** — アトミック書き込みの前に親フォルダを作成し、ファイル単位の非ネットワークエラーで残りファイルの同期が止まらないようにしました。

## [0.2.2] - 2026-06-09

- **衝突解決ポリシー** — クリーンに自動マージできない衝突時の挙動を選択可能に: 両側を一切触らず次回再試行（**エラー**＝既定）／**ローカル優先**／**リモート優先**／**コンフリクトマーカー**。設定画面で選択できます。
- **マージ対象をテキストファイルに限定** — テキストマージの対象は設定可能な拡張子（既定 `md`・`txt`）のみ。それ以外（画像・PDF・バイナリ等）はマージせず、コンフリクトマーカーでファイルを破壊することがなくなりました。衝突時の既定は `<<<<<<<` マーカー埋め込みから **エラー**（スキップして次回同期で再試行）に変更されています。

## [0.2.0] - 2026-06-09

Nextcloud 特化同期エンジンの初回公開リリース（0.2.0 〜 0.2.1）:

- **端末間の削除が確実に伝播** — ある端末で削除したファイルが次回同期で復活する問題を修正（sync-token 処理の修正。削除は内容検証付き・ゴミ箱経由で復元可能、大量削除の安全ガード付き）。
- **自動マージの結果がサーバーに反映** — マージ結果をアップロードして全端末が収束。同じコンフリクトが毎回再発しなくなりました。
- **モバイル診断** — Debug ログがモバイルでも端末別 `nextcloud-sync_debug_<端末>.txt` を出力し、Sync now が結果通知を表示。
- **モバイル（iOS / Android）対応** — モバイルでも動作するようになりました。プラットフォーム対応の分岐によりデスクトップの挙動は不変です。
- **クリック可能なステータスバー → 同期ステータスダイアログ** — ステータスバーをクリックすると、現在の同期状態・前回同期・未解決コンフリクト（0.2.6 以降はファイル単位のエラー詳細も）をまとめたダイアログが開きます。
- **「Sync now」を設定画面の最上部へ移動**し、認証済みのときのみ有効化。サインインすればすぐに同期を実行できます。
- **プラットフォーム対応のデフォルト設定** — 通信並列数・起動時同期・最大ファイルサイズなどの既定値をプラットフォームごとに最適化。
- **YAML フロントマターも自動マージ** — 重複しないフロントマターの編集は diff3 でクリーンに統合し、同一行を双方が変更した場合のみコンフリクトマーカーにフォールバックします。
- **Dry-run でのコンフリクト結果の明確化** — 初回同期プレビューがコンフリクト解決の結果を説明し、各コンフリクトファイルをクリックするとマージ後の内容（変更前後）をプレビューできます。
- **汎用 WebDAV より高速な同期** — 内容ハッシュと Nextcloud の `sync-token` を突き合わせ、毎回リモートツリー全体を再帰的に走査するのではなく、実際に変更された分だけを転送します。更新日時ベースの WebDAV プラグインより同期が明確に速くなります。

[1.0.1]: https://github.com/yanqingwang/obsidian-nextcloud-sync-yanc/releases/tag/1.0.1
[1.0.0]: https://github.com/yanqingwang/obsidian-nextcloud-sync-yanc/releases/tag/1.0.0
[0.7.43]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.43
[0.7.42]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.42
[0.7.41]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.41
[0.7.40]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.40
[0.7.39]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.39
[0.7.38]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.38
[0.7.37]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.37
[0.7.36]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.36
[0.7.35]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.35
[0.7.34]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.34
[0.7.33]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.33
[0.7.32]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.32
[0.7.31]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.31
[0.7.30]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.30
[0.7.29]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.29
[0.7.28]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.28
[0.7.27]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.27
[0.7.26]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.26
[0.7.25]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.25
[0.7.24]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.24
[0.7.23]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.23
[0.7.22]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.22
[0.7.21]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.21
[0.7.20]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.20
[0.7.19]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.19
[0.7.18]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.18
[0.7.17]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.17
[0.7.16]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.16
[0.7.15]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.15
[0.7.14]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.14
[0.7.13]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.13
[0.7.12]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.12
[0.7.11]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.11
[0.7.10]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.10
[0.7.9]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.9
[0.7.8]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.8
[0.7.7]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.7
[0.7.6]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.6
[0.7.5]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.5
[0.7.4]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.4
[0.7.3]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.3
[0.7.2]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.2
[0.7.1]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.1
[0.7.0]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.7.0
[0.6.1]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.6.1
[0.6.0]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.6.0
[0.5.0]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.5.0
[0.4.1]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.4.1
[0.4.0]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.4.0
[0.3.1]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.3.1
[0.3.0]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.3.0
[0.2.10]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.10
[0.2.9]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.9
[0.2.8]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.8
[0.2.7]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.7
[0.2.6]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.6
[0.2.5]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.5
[0.2.4]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.4
[0.2.2]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.2
[0.2.0]: https://github.com/siosig/obsidian-nextcloudsync/releases/tag/0.2.0
